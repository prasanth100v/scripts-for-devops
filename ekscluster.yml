apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: eks-cluster2
  region: ap-south-1
  version: "1.35"

managedNodeGroups:
  - name: workers
    instanceType: c7i-flex.large
    desiredCapacity: 2
    minSize: 2
    maxSize: 2
